class BudgetItem {
    id
    title
    value
    category

    constructor(id, title, value, category) {
        this.id = id
        this.title = title
        this.value = value
        this.category = category
    }
}